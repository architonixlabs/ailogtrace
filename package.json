{
  "name": "ailogtrace-monorepo",
  "private": true,
  "type": "module",
  "description": "Flight recorder for AI-assisted development — local-first audit and workflow-trace capture for Claude Code.",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/architonixlabs/ailogtrace.git" },
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "tsc -b --pretty"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
